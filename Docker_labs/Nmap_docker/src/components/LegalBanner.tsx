import { AlertCircle } from "lucide-react";
import { Alert, AlertDescription } from "@/components/ui/alert";

export const LegalBanner = () => {
  // Legal banner removed per request — keep component present in case pages import it.
  return null;
};
