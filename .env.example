# PostgreSQL Connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=

# Optional: Root database for initial setup
PGROOTDATABASE=postgres

# JWT Secret (change in production!)
JWT_SECRET_LOCAL=your-super-secret-key-change-in-production

# Optional: Default IP for test users
DEFAULT_IP_ADDRESS=0.0.0.0

TRAEFIK_NETWORK=traefik-net

# Host URL (main Next.js app)
HOST_URL=http://localhost:3000

# Lab Origin URL (Traefik proxy port for lab containers - change if you modify Traefik port)
LAB_ORIGIN_URL=http://localhost:20015

TRAEFIK_NETWORK=traefik-net


HOST_IP=localhost
HOST_PORT=3000

